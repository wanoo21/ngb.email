import { EmailBuilderDynamicDirective } from './i-p-email-builder-dynamic.directive';

describe('IPEmailBuilderDynamicDirectiveDirective', () => {
  it('should create an instance', () => {
    const directive = new EmailBuilderDynamicDirective();
    expect(directive).toBeTruthy();
  });
});
