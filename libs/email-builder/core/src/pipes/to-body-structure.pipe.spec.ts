import { ToBodyStructurePipe } from './to-body-structure.pipe';

describe('ToBodyStructurePipe', () => {
  it('create an instance', () => {
    const pipe = new ToBodyStructurePipe();
    expect(pipe).toBeTruthy();
  });
});
