import { IPEmailBuilderBlockDataPipe } from './ipemail-builder-block-data.pipe';

describe('IPEmailBuilderBlockDataPipe', () => {
  it('create an instance', () => {
    const pipe = new IPEmailBuilderBlockDataPipe();
    expect(pipe).toBeTruthy();
  });
});
