import { ColumnStylesPipe } from './column-styles.pipe';

describe('ColumnStylesPipe', () => {
  it('create an instance', () => {
    const pipe = new ColumnStylesPipe();
    expect(pipe).toBeTruthy();
  });
});
