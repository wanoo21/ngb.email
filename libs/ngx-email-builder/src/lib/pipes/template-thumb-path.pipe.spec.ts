import { TemplateThumbPathPipe } from './template-thumb-path.pipe';

describe('TemplateThumbPathPipe', () => {
  it('create an instance', () => {
    const pipe = new TemplateThumbPathPipe();
    expect(pipe).toBeTruthy();
  });
});
