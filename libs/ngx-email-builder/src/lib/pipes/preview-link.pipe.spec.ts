import { IpPreviewLinkPipe } from './preview-link.pipe';

describe('PreviewLinkPipe', () => {
  it('create an instance', () => {
    const pipe = new IpPreviewLinkPipe();
    expect(pipe).toBeTruthy();
  });
});
