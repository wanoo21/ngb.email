import { AbstractEmailBuilderDropList } from './abstract-email-builder-drop-list';

describe('AbstractEmailBuilderDropList', () => {
  it('should create an instance', () => {
    expect(new AbstractEmailBuilderDropList()).toBeTruthy();
  });
});
