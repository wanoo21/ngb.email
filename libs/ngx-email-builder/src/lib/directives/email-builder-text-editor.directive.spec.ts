import { IPEmailBuilderTextEditorDirective } from './email-builder-text-editor.directive';

describe('EmailBuilderTextEditorDirective', () => {
  it('should create an instance', () => {
    const directive = new IPEmailBuilderTextEditorDirective();
    expect(directive).toBeTruthy();
  });
});
