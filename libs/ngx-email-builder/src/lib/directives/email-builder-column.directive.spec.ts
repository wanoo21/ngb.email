import { IPEmailBuilderColumnDirective } from './email-builder-column.directive';

describe('EmailBuilderColumnDirective', () => {
  it('should create an instance', () => {
    const directive = new IPEmailBuilderColumnDirective();
    expect(directive).toBeTruthy();
  });
});
