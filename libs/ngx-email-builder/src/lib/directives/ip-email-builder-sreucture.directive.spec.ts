import { IpEmailBuilderStructureDirective } from './ip-email-builder-structure.directive';

describe('IpEmailBuilderSreuctureDirective', () => {
  it('should create an instance', () => {
    const directive = new IpEmailBuilderStructureDirective();
    expect(directive).toBeTruthy();
  });
});
