import { IPEmailBuilderStructuresDirective } from './email-builder-structures.directive';

describe('EmilBuilderColumnDirective', () => {
  it('should create an instance', () => {
    const directive = new IPEmailBuilderStructuresDirective();
    expect(directive).toBeTruthy();
  });
});
