import { IPHistoryDirective } from './history.directive';

describe('HistoryDirective', () => {
  it('should create an instance', () => {
    const directive = new IPHistoryDirective();
    expect(directive).toBeTruthy();
  });
});
