import { MarkdownPipe } from './markdown.pipe';

describe('MarkdownPipe', () => {
  it('create an instance with Http injection', () => {
    // const pipe = new MarkdownPipe();
    // expect(pipe).toBeTruthy();
  });
});
